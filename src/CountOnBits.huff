/**
 *  COUNT ON BITS HUFF
 *  
 *  The task is to create a HUFF contract with a function countOnBits(uint256)
 *  to count how many bits are on in a number.
 *  
 *  NOTICE: The contract should revert when an unrecognized function is called
 */

#define function countOnBits(uint256 number) pure returns (uint8)

#define macro MAIN() = takes(0) returns(0) {

}

